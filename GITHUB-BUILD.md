# ساخت فایل exe در فضای ابری با GitHub (بدون نصب هیچ‌چیز)

با این روش GitHub خودش روی سرورِ ویندوز برنامه را می‌سازد و فایل نصبی را
به تو تحویل می‌دهد. روی کامپیوترِ خودت به Node یا هیچ ابزاری نیاز نداری —
فقط یک مرورگر.

## یک‌بار: ساختن مخزن
1. در github.com یک حساب بساز (رایگان).
2. دکمه‌ی سبزِ **New** یا علامتِ + بالا سمت راست ← **New repository**.
   - یک اسم بده، مثلاً `tooltrace`
   - **Private** را انتخاب کن (تا کدت عمومی نشود)
   - **Create repository**
3. در صفحه‌ی مخزن، لینکِ **uploading an existing file** را بزن، یا
   **Add file ← Upload files**.
4. کلِ محتویاتِ همین پوشه را (نه خودِ پوشه، بلکه فایل‌ها و پوشه‌های داخلش:
   `main.js`, `preload.js`, `package.json`, پوشه‌ی `app`, و پوشه‌ی `.github`)
   بکش و روی صفحه رها کن.
   - نکته: پوشه‌ی `.github` مخفی به‌نظر می‌رسد ولی باید آپلود شود؛ اگر
     drag & drop نگرفت، مخزن را با گزینه‌ی Git هم می‌شود پر کرد.
5. پایین صفحه **Commit changes**.

## هر بار که می‌خواهی فایل نصبی بسازی
1. در مخزن، تبِ **Actions** بالای صفحه.
2. اگر پیام اجازه خواست، **I understand my workflows, enable them**.
3. از لیستِ سمت چپ **Build Equip360 for Windows** را انتخاب کن.
4. دکمه‌ی **Run workflow** (سمت راست) ← دوباره **Run workflow**.
5. حدود ۳ تا ۵ دقیقه صبر کن تا تیکِ سبز بیاید.
6. روی همان اجرا کلیک کن، پایین صفحه بخشِ **Artifacts** ←
   **Equip360-Windows** را دانلود کن. داخلش فایلِ نصبیِ exe است.

## به‌روزرسانی برنامه
فایل `app/Equip360.html` را در مخزن با نسخه‌ی جدید جایگزین کن
(Add file ← Upload files، همان اسم را نگه دار)، شماره‌ی `version` را در
`package.json` بالا ببر، و دوباره Actions ← Run workflow.

## نکته درباره‌ی نسخه‌ی محافظت‌شده
فایلِ app/Equip360.html که اینجا هست همان نسخه‌ی مبهم‌سازی‌شده است؛
کدش خوانا نیست. اگر خواستی نسخه‌ی جدید بسازی، از همان فایلِ محافظت‌شده استفاده کن.

---

## محافظت از کد (خودکار)
هنگام ساخت، اسکریپت `obfuscate.js` به‌طور خودکار اجرا می‌شود و کدِ برنامه را
درهم و رمزگذاری می‌کند (فایلِ `app/app.bundle.html`). فقط همین نسخه‌ی درهم‌شده
داخلِ فایل نصبی قرار می‌گیرد؛ نسخه‌ی خوانا (`app/Equip360.html`) هرگز در exe
گنجانده نمی‌شود. هیچ کارِ دستی لازم نیست — `npm run dist` خودش انجامش می‌دهد.

---
© 2026 Jafar Shahrokhi. All rights reserved.
