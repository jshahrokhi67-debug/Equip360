# Equip360 — ساخت نسخه‌ی نصبی ویندوز

## پیش‌نیاز (یک‌بار)
1. Node.js نسخه‌ی LTS را از nodejs.org نصب کن (گزینه‌های پیش‌فرض کافی است).
2. برای اطمینان، در Command Prompt بزن: `node -v` — باید شماره‌ی نسخه بدهد.
3. اینترنت لازم است؛ بار اول حدود ۳۰۰ مگابایت دانلود می‌شود. اگر دسترسی محدود است،
   قبل از شروع در همان Command Prompt بزن:
   `set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`

## ساخت
پوشه را از حالت zip خارج کن، داخلش Command Prompt باز کن (در نوار آدرسِ File Explorer
بنویس `cmd` و Enter) و این دستورها را به ترتیب بزن:

```
npm install
npm start          ← برای امتحان، برنامه باز می‌شود
npm run dist       ← ساخت فایل نصبی
```

خروجی در پوشه‌ی `dist` می‌آید: **Equip360-Setup-1.0.0.exe**

دستورهای دیگر:
- `npm run portable` — یک فایل exe تکی بدون نصب، مناسب فلش‌مموری
- `npm run dist32` — برای ویندوزهای ۳۲ بیتی قدیمی

## به‌روزرسانی برنامه
فایل `app/Equip360.html` را با نسخه‌ی جدید جایگزین کن، در `package.json` شماره‌ی
`version` را بالا ببر و دوباره `npm run dist` بزن.

## نکته‌ی مهم درباره‌ی هشدار ویندوز
چون فایل امضای دیجیتال (Code Signing) ندارد، ویندوز هنگام نصب پیام
«Windows protected your PC» می‌دهد. مشتری باید روی **More info** و بعد
**Run anyway** بزند. برای حذف این پیام باید گواهی Code Signing بخری
(سالانه‌ای، از صادرکننده‌های بین‌المللی) — برای فروش به شرکت‌ها ارزشش را دارد،
ولی ضروری نیست.

## داده‌ها کجا ذخیره می‌شوند
`C:\Users\<نام کاربر>\AppData\Roaming\Equip360`
با کلید **Ctrl+Shift+D** داخل برنامه همین پوشه باز می‌شود.
حذف برنامه داده‌ها را پاک نمی‌کند. با **F12** کنسول برای عیب‌یابی باز می‌شود.

## محافظت از کد
`npm run dist` قبل از ساخت، به‌طور خودکار `obfuscate.js` را اجرا می‌کند: کدِ
برنامه درهم و رشته‌ها رمزگذاری می‌شوند و نسخه‌ی خوانا در فایلِ نصبی قرار نمی‌گیرد.
برای به‌روزرسانی، همان `app/Equip360.html` را عوض کن؛ درهم‌سازی خودکار است.

---
© 2026 Jafar Shahrokhi. All rights reserved.
